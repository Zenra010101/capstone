namespace GensanPOS.Domain.Enums;

/// <summary>
/// Operational resellable return only (wrong item/size/spec or approved exchange).
/// Store policy rejects defective, broken, used, and warranty returns — not modeled here.
/// </summary>
public enum ReturnItemCondition
{
    Good = 0
}
