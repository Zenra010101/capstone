# Phase 4 — complete

Phase 4 frontend and backend are implemented. Delete `gensanpos.db` and restart the API after schema changes.

See README **Phase 4 API** section for endpoints and routes: `/returns`, `/adjustments`, `/sales?history=true`.
